# 隐私说明

Audio Memory 在用户自己的 Mac 上保存音频、转写、报告、Prompt、反馈和运行数据。原始音频用于本地转写，不发送给报告模型厂商；生成报告所需的文字内容会发送给用户主动配置的模型厂商。

API Key 仅保存在 macOS Keychain。Audio Memory 的数据库、日志、反馈文件和发布包不应包含明文 API Key。

用户数据默认位于 `~/Library/Application Support/AudioMemory`。重新安装或升级程序不会主动删除该目录；彻底删除数据需要用户另行确认。

