# 更新日志

## 0.1.0-beta.7-hotfix.2

- 修复 AAC 音频的云端转写时长略长于本机检测时长时，有效末尾语句被误判为越界并触发 `cloud_asr_failed` 的问题。
- 仅在云端时长偏差不超过 1% 时接受该时间轴，更大的异常偏差仍会失败关闭。
- 云端转写失败后可从原位置继续，不重复处理已完成的文件。

## 0.1.0-beta.7-hotfix.1

- 模型配置只保留 DeepSeek V4 Pro，移除 Kimi、OpenAI 和已隐藏的 GLM 配置入口。
- 后端同步拒绝已下线厂商的配置、校验、激活和模型切换请求，避免绕过界面创建新任务。
- 旧安装如仍激活其他厂商，当已配置的 DeepSeek Key 校验通过时自动迁移到 DeepSeek V4 Pro；历史报告的模型来源信息保留。
- 发版前增加固定 MP3 + AAC 真实双音频门禁，必须完成上传、转写、DeepSeek V4 Pro 分析和报告发布。

## 0.1.0-beta.7

- 用户无需配置 OSS：客户端通过设备注册获取短期、单文件授权，长期 OSS 密钥不会写入安装包。
- 修复首次云端转写可能直接报 `managed_storage_unavailable`；失败后保留本机音频，并可从云端转写阶段继续。
- 增加设备签名、请求防重放、使用限额和安全账本，收紧托管存储服务的访问边界。
- 首次安装默认选择 DeepSeek V4 Pro；未激活当前模型时不再误显示为可上传，创建任务失败会明确提示原因。
- 报告生成与语音转写配置文案更清晰，“分析已经开始”提示每个应用版本只显示一次。
- 升级安装复用共享依赖缓存，减少重复下载时间；完整继承 Beta 6 的火山云端转写、报告恢复和审计修复。

## 0.1.0-beta.6

- 新增火山引擎“豆包录音文件识别 2.0”云端转写；用户需同时配置内容分析 API 和火山语音 API 后才能上传 MP3/AAC 音频。
- 开始分析后自动完成云端转写和内容分析；中途无需再操作，上传、转写与报告进度会持久化展示。
- 音频通过受控的阿里云 OSS 临时链接交给火山，转写完成后自动删除云端临时文件；用户无需配置 OSS。
- 云端转写和分析失败时最多自动重试 2 次，支持从失败位置继续，不重复调用已完成的阶段。
- 补充关闭页面、电脑重启和自动休眠可能中断分析的提示，优化火山 API 校验时间、云端转写文案、进度恢复和报告页左侧固定布局。
- 修复 Kimi K3 审计结果使用 `opp_` 内部 ID 缩写时整单失败，以及分析版本已失败但上层任务状态滞后时无法重试的问题。
- 完整继承 Beta 5 Hotfix 1–4 的长报告截断恢复、审计结果恢复、分数修正与不稳定模型下架修复。

## 0.1.0-beta.5-hotfix.4

- DeepSeek 新任务只允许选择 V4 Pro，移除已知审核不稳定的 V4 Flash。
- Kimi 新任务只允许选择 K3，移除 Kimi 2.6。旧安装保存的已移除模型会回退到对应厂商的高质量默认模型，历史报告不受影响。

## 0.1.0-beta.5-hotfix.3

- 修复 DeepSeek V4 Flash 生成长报告时用尽输出 Token 导致整单失败的问题；现在限制报告篇幅，并在初稿截断时有界重写一次。
- Flash 审核改用小块和本地保守合并；审核经有限恢复仍失败时，保留并发布完整 V1，标记为“已完成（未审计）”，不再让用户整单失败。

## 0.1.0-beta.5-hotfix.2

- 修复 DeepSeek V4 Flash 返回的审计分项分数合法、但 `total` 加总错误时报告失败的问题；现在由程序基于五个分项确定性重算总分，其他质量门槛保持不变。

## 0.1.0-beta.5-hotfix.1

- 修复 DeepSeek V4 Flash 在最终合并审计结果不是合法 JSON 时直接失败的问题；现在会复用已完成的报告和分段审计，仅重试一次最终合并。

## 0.1.0-beta.5

- 修复报告审计输出过长时在固定拆分深度后失败的问题；现在会保留转写和初稿，并继续拆分审计直到可恢复完成。
- 重复点击“重新分析”时复用已运行的任务，不再返回 500；分析阶段失败时调用正确的重试接口，不再误用转写恢复。
- 增加审计拆分、恢复结果和重复重试合并的脱敏结构化日志，不记录 API Key、转写、Prompt 或模型原文。

## 0.1.0-beta.4

- 修复发布包在转写完成后进入模型分析时，因读取未打包的旧 Prompt 文档而失败的问题。
- 运行时指纹改为仅覆盖当前正式报告的生成、审核与修订 Prompt 及 Schema。

## 0.1.0-beta.3

- 修复转写完成后分析任务未持久化入队、页面长期停在“正在分析”的问题，并增加锁竞争恢复和脱敏结构化日志。
- 报告生成进度可持久化展示“生成初稿、审校报告、修订报告、最终审校、发布报告”，发布完成后会刷新并展示报告。
- 任务从转写到报告完成期间，后端统一阻止新建、新上传、并发启动和进行中音频删除。
- 修复中断任务的“继续分析”和“取消任务”：支持从持久化断点恢复，取消时只清理本次上传的音频与进度，不影响历史记录。
- API 配置中 DeepSeek 排在 Kimi 之前；所有 Toast 统一在页面中央显示，并明确发布阶段不可取消的边界。

## 0.1.0-beta.2

- 发布包自带 uv、ffmpeg 和 ffprobe，用户无需安装 Homebrew、Python、Node.js 或系统 FFmpeg。
- 安装时创建应用私有 Python 环境，并校验随包运行组件；已存在且有效的资源会复用。
- 修复有效 MP3/AAC 因本地音频工具缺失而被误报为格式不支持的问题。
- 开发环境与正式安装环境使用独立端口、数据目录、运行目录和 Keychain 配置，避免互相影响。
- 分析音频期间阻止 Mac 自动休眠，任务结束后自动释放。

## 0.1.0-beta.1

- 首个终端安装 Beta 版本。
- 支持在 macOS Apple Silicon 本地上传和转写音频。
- 支持使用用户配置的模型生成、审核并发布综合报告。
- 保留本机历史音频、转写、报告、Prompt 与反馈数据。
- 支持 GLM、Kimi、DeepSeek 和 OpenAI 的预设模型选择。
- 发布包自带 uv、ffmpeg 和 ffprobe，不要求用户安装 Homebrew、Python 或 Node.js。
- 本地音频组件故障不再被误报为 MP3/AAC 格式不支持。
