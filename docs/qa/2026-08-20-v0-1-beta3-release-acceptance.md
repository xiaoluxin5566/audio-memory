# Audio Memory v0.1.0-beta.3 发布验收记录

## 发布边界

- 候选版本：`v0.1.0-beta.3`
- 发布分支：`codex/release-v0-1-beta3`
- 集成基线：`main@1fb86a5bb4b3237a02e5b5e8ec43a4e56e424914`
- 用户已安装的 `v0.1.0-beta.2` 、正式数据目录、正式 Keychain 和 8765 服务不在本次准备操作范围内。
- 安装、升级、回滚和数据保留测试只使用临时 HOME 和临时端口。
- 本分支不直接创建标签、不推送 GitHub、不创建 GitHub Release；这三项必须等最终验收证据完成后再由用户确认。

## 本版本范围

- 转写到分析的持久化交接、并发锁恢复和结构化日志。
- 分析进度持久化与报告发布后页面刷新。
- 任务进行期间的新建、上传、启动和文件删除锁。
- 中断任务的续传、取消、本次音频清理和 provider 快照恢复。
- DeepSeek/Kimi 配置顺序、取消发布阶段边界与页面中央 Toast。

## 待填写验收证据

- [ ] 发布治理候选摘要与集成回执（发布分支合并回 `main` 后生成）
- [x] 后端、前端、浏览器和环境隔离全量门禁
- [x] 安装包 SHA-256 和内容白名单审计
- [x] 隔离 HOME 全新安装
- [x] 隔离 HOME 从 beta.2 升级、备份和历史保留
- [x] 隔离 HOME 回滚 beta.2
- [x] 受控假 provider 端到端发布路径
- [x] 已安装 beta.2 正式环境未被修改的只读证据

## 自动化门禁

2026-08-20 在发布分支的干净提交上运行 `scripts/quality-gate.sh`：

- 后端：1189 项通过，28 项旧 Event Map 兼容用例按设计跳过。
- 前端：101/101 通过，Vite 生产构建成功。
- 真实浏览器：32/32 通过。
- 运行环境隔离：19/19 通过。

门禁在新 worktree 首次运行时发现 Sites 包测试依赖旧 `dist`。新增失败测试后，门禁改为先产生全新前端构建、再执行检查，从而不再依赖工作区残留文件。

## 安装包审计

- 文件：`audio-memory-v0.1.0-beta.3-macos-arm64.tar.gz`
- SHA-256 由同目录 `.sha256` 文件与 GitHub Release 资产共同发布；最终值在候选提交固定后生成，不自引用写入该提交。
- 归档共 206 个目录项，唯一根目录为 `audio-memory-v0.1.0-beta.3`。
- 包内 `uv`、`ffmpeg 8.0.1` 和 `ffprobe 8.0.1` 均为 arm64，运行时校验通过，必要脚本保留可执行权限。
- 未发现 Git/worktree/治理记录、运行数据、数据库、音频、日志、密钥、测试、模型或依赖缓存。

## 全新安装

在 `/private/tmp` 创建全新 HOME 和数据目录，使用包内 `uv` 下载 arm64 CPython 3.12.13 并按锁文件安装 55 个运行依赖。模型本体下载在本项验收中显式关闭。

首次真实安装暴露旧安装器在 `.install-*` 临时目录中创建虚拟环境后再移动目录，导致包路径失效。新增回归用例后，安装器改为在最终版本目录准备环境；失败不切换 `current`，重试可继续。

修复后重新从包安装，验证结果：

- CLI 输出 `Audio Memory 0.1.0-beta.3`。
- 最终版本目录的 Python 可直接导入 `audio_memory==0.1.0-beta.3`。
- 隔离后端 `/api/health` 返回 `status=ok`、`profile=production`、`version=0.1.0-beta.3`。
- SQLite 迁移到 `0014`，共 20 张表。

## 升级、备份与回滚

另一个全新临时 HOME 先安装 beta.2，写入历史数据库行、音频文件和报告文件，再用 beta.3 安装器升级：

- `current` 正确切换为 beta.3。
- 主数据库中的历史行保留。
- 升级前备份数据库可读，包含同一历史行。
- 音频与报告文件的 SHA-256 在升级前后一致。
- 将 `current` 回滚到保留的 beta.2 版本目录后，版本与历史行仍可读。

## 功能路径与正式环境

完整浏览器门禁使用受控假 provider 覆盖上传、转写交接、持久化分析进度、报告发布刷新、上传/删除锁、中断续传/取消和页面中央 Toast。默认发布验收不读取真实 Keychain，不调用真实模型。

已安装正式环境只读核对：

- `current` 仍指向 `0.1.0-beta.2`。
- 正式数据库大小为 17,940,480 字节，修改时间为 `2026-08-19T19:29:48+0800`。
- 正式数据库 SHA-256 为 `a31e690bc8590d45d85ce7e4a108420e82ec3b2cac3b69de3366e74f78c7dfc1`。
- 8765 无监听进程；本次没有启动、停止或更改正式服务。
