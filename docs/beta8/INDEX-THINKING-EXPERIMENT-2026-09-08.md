# Pro 索引开启深度思考：单变量验证

用户已明确同意将索引深度思考打开；对照基线为 `run-8ad367fd-0b46-459f-b609-0d52ebf9d937`。

仅将索引请求的 `thinking_enabled` 从 `false` 改为 `true`。模型仍是 `deepseek-v4-pro`，输出预算仍为 32,000 Token，超时 300 秒，底层请求尝试总数 1。完整逐字稿、Schema、两个 Prompt、规范化和验收逻辑均不变。

索引 Prompt SHA-256：`01ca85c743c7b66faee19ce3eac6679251306429aea6b26a91a6c700334edbb9`。

V1 Prompt SHA-256：`4073253328a8275c7fd8e92e58bd54247878c0d43d719b9e062a35d9ce739bd0`。

新固定规则 SHA-256：`7ae6810dd3016c81b27e1343e8af3755b6a9e38abbbd522b2a6f981d7c2fb35b`。

旧固定规则 SHA-256：`60695d1684c96fa24865dc57b241df720153d818290b45ac3e9f9b130d6c732f`。

先调用一次真实索引并暂停；自动门禁和阅读复核通过后，才复用本次检查点调用完整 V1。V1 本来已开启思考，本轮不改变其参数。失败时保留原文和计量，不自动重购索引，不运行审核、搜索或发布。

离线对比确认请求只有思考开关变化；参数传递、Runner 和供应商请求相关回归 144 项通过（1 项未选择）。固定规则哈希随思考设置变化，避免复用与当前设置不一致的检查点。

## 已执行结果

运行 `run-9b934759-506e-4484-bf8e-6745bfaf9eb1`：1 次调用，输入 237,726 Token、输出 12,663 Token，模型耗时 227,197 ms，无超时或截断。结构规范化通过但内容门禁失败；未运行 V1。

完整证据见该运行目录 `acceptance-attempt-12-thinking-index.md`。随后免费修复了所有活动首尾证据的邻近检查，当前固定规则哈希变为 `d146300a558b02d27043b2329e9b5272dce5ab4780f420df272bf2e262168308`；本次真实运行仍对应上文的 `7ae6810...`，没有修改历史记录或再次调用模型。
