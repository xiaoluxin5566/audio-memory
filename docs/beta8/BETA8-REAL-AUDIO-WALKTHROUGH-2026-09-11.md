# Beta 8 真实音频走查（2026-09-11）

## 结论

本次使用 `通话录音_2026_09_08.aac`（28 分 56.6 秒）按用户正常链路完成了一次云端转写和 P1–P5 报告生成。转写、报告发布、卡片详情、评分展示和原音证据回听均已跑通。

但这份结果不能作为 Beta 8 质量封板证据：P2 漏掉了应当搜索的公开方法，P4 存在一处人名错写，P5 没有发现该错误且给出 100 分。链路可运行，当前报告质量验收不通过。

## 输入与运行身份

- 音频：`/Users/liujinxin/Downloads/通话录音_2026_09_08.aac`
- 时长：1736.58 秒（28 分 56.6 秒）
- 转写结果：304 个 segment
- Job ID：`1376133b-f80d-44b4-ad4e-219ae2ec342e`
- Version ID：`b1cb2274-c4d0-495c-9c2a-a996a3709953`
- 最终状态：`scored_v1`，已发布 4 张卡

## P1 结果

P1 本次只需 1 个窗口，因此调用 1 次。窗口数由转写文本的 UTF-8 字节量与 80 KB 核心窗口预算决定，音频分钟数只是间接因素。

- Activities：2 个
  - 会前协调与等待（`call_setup_and_waiting`）
  - DR 评测流程迁移与自动化项目会议（`work_meeting`）
- Topics：7 个
  - 平台使用和配置
  - 12 个评测任务的平台/本地拆分
  - 新旧链路指标对比
  - 模型替代人工标注及 5%–10% 抽检
  - DR 评测文档和 SOP
  - 本周时间表
  - 本地代码运行方式
- Attention Signals：8 个（`important_change` 1、`completed` 2、`blocker` 1、`deadline` 1、`decision` 2、`risk` 1）
- Commitments：4 个

P1 本次没有出现 topic range 和 anchor 的边界矛盾，也没有丢失主会议内容。

## P2 结果

P2 调用 1 次，遍历了 7 个 topic；2 个 topic 独立出卡，5 个 topic 合并进其他卡，没有 topic 被删除。共规划 4 张卡：

1. 平台与本地任务如何划分、如何运行
2. 如何在本周完成新旧链路指标对比
3. 如何用模型替代人工标注并设计 5%–10% 抽检
4. 如何产出 DR 评测文档和可复用 SOP

P2 生成 0 个搜索任务。根因是它把“内部项目事实不能靠公开搜索回答”错误扩展成了“公开方法也不需要搜索”。第 3 张卡承诺回答抽检比例、样本选取和置信阈值，但原文明确说这一机制还没研究，因此应当生成公开权威方法搜索任务。

## P3 结果

P3 没有执行：P2 未生成任何 research task，因此 Kimi 调用数、搜索工具调用数、验证来源数均为 0。这不是 Kimi 排序或网络失败，是 P2 的上游搜索规划失败。

## P4 与 P5 结果

| 主卡 | P5 分数 | 验收 | 质量发现 |
|---|---:|---|---|
| DR 评测主走本地、少数上平台 | 100 | 通过 | 结构和行动性较好 |
| 新旧链路指标对比周 | 100 | 通过 | 与 SOP 卡有一定重叠，P2 未充分合并 |
| 模型替代人工标注 | 87 | 未通过 | 缺少抽检设计方法和阈值决策机制 |
| DR 评测文档与 SOP | 100 | 通过 | 把原文的“小刚”错写为“小王”，P5 未扣分 |

P5 当前是“每张主卡单独评分”。这能找到卡内分析和行动缺口，但不能发现跨卡重复；跨卡重复必须由 P2 的候选卡两两检查阻止。

## 实际调用记录

- 云端 ASR：1 个转写任务，未重复提交
- P1：1 次
- P2：1 次
- P3：0 次
- P4：4 次成功 + 1 次响应中断且计费状态不确定的请求
- P5：4 次
- 报告请求意图合计：11 次；产生有效阶段结果 10 次

第三张 P4 曾在高强度思考期间连接中断，但请求没有返回 `finish_reason=length`，无法据此认定超过 24,000-token 输出上限；同轮另外两张 P4 在 `high` 下正常完成。为优先保障报告分析质量，当前 P1/P2/P4 均使用 `high`，P4 保留 24,000-token 总生成上限，P5 关闭思考。链路不会自动重发计费状态不确定的请求，只有用户明确点击重试后才会续跑。

## 已修复的链路问题

1. 嵌套制品目录无法创建。
2. P4 高强度思考曾发生长响应中断；保留高强度思考与 24,000-token 上限，通过单卡检查点和明确重试恢复控制风险。
3. 不确定请求缺少“明确重试”后的安全恢复路径。
4. V1 卡片原音证据接口错误按旧场景结果校验，导致 404。
5. 页面未能从聚合统计恢复正常/重试调用数，详细调用和耗时未持久化。
6. 正常上传未传入录音文件修改时间和时区。

## 已完成但待真实复验的质量修复

- P2：区分“内部事实不可搜”与“公开方法应当搜”；增加候选卡两两重叠检查。
- P4：成文前逐项核对人名、时间、数字和责任人，禁止用常见人名替换原文实体。
- P5：将人名、时间、数字和责任人精确一致性加入事实检查，不一致必须扣分。

这些 Prompt 修复已通过本地合同测试，但尚未用付费模型重新生成报告，因此不能声称质量闭环已验证。

## 封板判定

- 技术链路：通过。
- 报告质量：不通过。
- 当前是否可封板：否。
- 解除阻塞的条件：使用已保存的转写结果启动一个新分析版本，用新 P2/P4/P5 Prompt 跑完 P1–P5；必须看到有效搜索任务、无实体错写、P5 能正确扣分，再进入封板。

## 封板复验补记（2026-09-14）

在上述质量修复之后，使用同一份原始 AAC 按用户正常入口重新执行了完整流程，没有人工修改 P1/P2/P3/P4/P5 中间结果。

### 复验身份与结果

- 音频 SHA-256：`ffe8887e384c94ea26f705931664760f0547b1e52fcb6ec55710c728533fa9cf`
- 音频时长：1,736,579 ms（28 分 56.579 秒）
- Job ID：`93c9d659-b26a-4b67-9cb2-98cf4227f360`
- 豆包 ASR Task ID：`5ab4167f-77d7-4dd1-9e5a-5ba10f78ac6c`
- Analysis Version ID：`6c3a3a75-cfce-48f8-9d2d-3d8bfd996323`
- 转写：305 个 segment；临时云端对象已删除
- P1 / P2 / P4 / P5：各调用 1 次；P3：0 次
- 模型用量：输入 125,823 tokens，输出 29,036 tokens；模型耗时 402,647 ms
- 最终状态：`scored_v1`，发布 1 张主卡，P5 给出 100 分

### 封板判断

- 技术链路：通过。真实 AAC 已完成上传、豆包转写、P1、P2、P4、P5、发布与页面读取。
- 实体准确性：本次没有复现上一轮的人名替换错误。
- 搜索链路：本次 P2 判断无需外部搜索，因此 P3 没有调用；这证明“无搜索任务时正常跳过”的链路，但不构成 Kimi 在该样本上的搜索质量证明。
- 规划稳定性：本次 P2 将 9 个主题整合为 1 张主卡，与上一轮 4 卡结果存在明显差异。产品允许整合，但这种波动仍需在更多真实音频中观察，不能仅凭 100 分认定报告质量已经稳定。

因此，本次复验解除“真实用户流程跑不通”的封板阻塞；Beta 8 可以形成候选版本。P2 出卡数量与搜索触发稳定性作为已知质量风险记录，不伪装为已完全解决。
