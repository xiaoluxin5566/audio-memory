# Audio Memory 第一阶段产品 PRD

> 文档版本：v0.9（评审稿）  
> 更新日期：2026-08-04  
> 产品形态：macOS 本地运行的 PC Demo  
> 目标版本：用户体验版  
> 文档状态：待产品评审，评审完成后升级为 v1.0 开发基线

---

## 1. 执行摘要

Audio Memory 是一套运行在用户 Mac 本地的音频智能分析 Demo。用户通过终端安装和启动系统，在浏览器页面中上传已有音频文件；系统使用本地 Whisper 完成转写，再将转写文本发送给用户自行配置的 Kimi、DeepSeek 或 OpenAI，生成待办、会议纪要、家庭教育、内容推荐、成长建议和闲聊灵感等结构化结果。

第一阶段的目标不是实现真正的全天候录音，而是通过“上传已有音频 → 本地转写 → 多场景分析 → 信息流展示 → 反馈与追问”的完整闭环，验证真实环境音频中是否存在稳定、可感知的用户价值，以及不同场景 Prompt 是否能输出用户愿意持续查看的内容。

所有原始音频、转写、结果、问答和内部个人画像长期保存在用户本地；原始音频不发送给分析模型厂商。用户可配置并分别保存三个厂商的 API Key，选择其中一个厂商完成当前批次分析。

---

## 2. 背景与问题

### 2.1 用户问题

用户每天在会议、家庭互动、内容消费和随口讨论中产生大量高价值信息，但这些信息通常存在以下问题：

- 重要决策、待办和观点散落在长音频中，回顾成本高。
- 很多想法只在对话中短暂出现，事后容易遗忘。
- 传统转写工具只提供文字或摘要，无法覆盖会议、教育、成长和灵感等不同价值场景。
- 单次总结缺乏长期理解，推荐和建议难以随着音频积累变得更贴合用户。
- 云端音频处理容易引发隐私顾虑，尤其是家庭和全天环境音频。

### 2.2 产品机会

如果系统能够在本地持续积累音频和转写，并使用可配置的场景 Prompt 提取不同类型的价值，就有机会从“一次性转写工具”发展为“帮助用户理解生活与行动的个人音频记忆系统”。

### 2.3 第一阶段需验证的核心假设

1. 已有音频上传的方式足以验证 Always-on 场景的核心价值。
2. 同一份本地 Whisper 转写可以支持多个模型厂商生成可比较的端到端结果。
3. 用户愿意在时间线信息流中消费多种场景卡片。
4. 场景化 Prompt 能比单一摘要更稳定地发现待办、会议、教育、推荐、成长和灵感信息。
5. 隐藏的个人画像能够让推荐与建议随着音频积累变得更贴合用户。
6. 意见反馈记录能够形成后续优化 Prompt 和模型效果的本地样本。

---

## 3. 目标用户与使用任务

### 3.1 主要用户

第一阶段面向愿意主动上传日常录音、希望验证个人音频智能价值的 macOS Apple Silicon 用户。

用户特征：

- 能够通过终端安装和启动 Demo。
- 有会议、家庭沟通、内容消费或日常发散讨论等音频。
- 愿意自行提供模型厂商 API Key。
- 重视隐私，希望原始音频保存在本机。
- 更关注分析结果是否有用，而不是音频编辑和播放能力。

### 3.2 Jobs to Be Done

- 当我上传一批日常音频时，我希望系统自动发现其中值得保存的信息，而不需要逐段回听。
- 当音频中存在明确行动时，我希望待办被集中整理，方便继续处理。
- 当我回顾会议或家庭互动时，我希望看到符合该场景的结构化结果和建议。
- 当我消费内容或随口讨论想法时，我希望系统帮助我发现兴趣线索和有价值的灵感。
- 当模型结果不准确时，我希望能够留下反馈，帮助后续优化。
- 当我再次启动系统时，我希望历史音频和结果仍然完整存在本机。

---

## 4. 产品目标与非目标

### 4.1 第一阶段产品目标

- 建立一条可用的端到端本地音频分析链路。
- 验证五类内容卡片和全局待办的用户价值。
- 验证 Kimi、DeepSeek、OpenAI 作为分析模型的接入框架。
- 建立可增删改查的场景 Prompt 与 JSON Schema 框架。
- 建立本地持久化、任务恢复、意见反馈和隐藏个人画像基础。
- 为后续模型对比版和真正 Always-on 形态积累产品与模型样本。

### 4.2 第一阶段非目标

- 不接入眼镜、麦克风或后台实时录音。
- 不实现真正的 Always-on 采集。
- 不支持 Windows、Intel Mac、移动端。
- 不支持音频回听、剪辑、重命名或音频管理。
- 不支持历史音频搜索、筛选、删除或重新分析。
- 不提供用户可见的个人标签页或标签修改能力。
- 不提供模型对比版页面。
- 不提供推荐算法排序，卡片按时间线和固定顺序展示。
- 不允许模型直接决定页面结构或输出任意 Markdown。

---

## 5. 产品原则

1. **本地优先**：原始音频、转写和结果长期保存在本机。
2. **音频不上云**：分析厂商只接收 Whisper 转写文本。
3. **整批原子提交**：结果全部完成后才进入历史和信息流。
4. **有内容才生成**：未识别到有效场景时不展示空卡片。
5. **事实与推测分开**：人物关系、情绪原因、问题原因等必须标明为模型推测。
6. **统一框架、场景可扩展**：详情页外壳一致，场景内容由 Prompt、Schema 和模块配置决定。
7. **用户控制关键结果**：待办可编辑、完成和删除；模型反馈可留存。
8. **不暴露内部画像**：画像用于个性化，但第一阶段不向用户展示内部标签。
9. **失败不污染历史**：未完成任务按未上传处理，不进入正式历史。

---

## 6. 信息架构

### 6.1 顶部导航

- 信息流
- 音频历史
- 清除所有历史

第一阶段不展示“个人标签”入口。

### 6.2 首页

左侧：

- 模型厂商与 API Key
- 音频上传
- 上传、转写、分析、失败和恢复状态

右侧：

- 全局待办
- 按自然日和上传批次排列的信息流
- 五类内容卡片

### 6.3 五类内容卡片

1. 会议纪要
2. 家庭教育
3. 内容推荐
4. 成长建议
5. 闲聊灵感

全局待办不是批次卡片，固定在信息流最顶部。

---

## 7. 核心用户流程

```text
终端安装并启动
  → 选择模型厂商
  → 填写 API Key
  → 保存并校验
  → 上传一批 MP3 / AAC 音频
  → 文件逐个上传到本机并展示进度
  → 本地 Whisper 逐个转写
  → 当前模型分析整批转写文本
  → 更新隐藏个人画像
  → 原子保存音频、转写、卡片、待办和问答
  → 右侧一次性展示新结果
  → 左侧立即清空并恢复默认上传状态
```

失败、中断或取消的批次不进入正式历史。

---

## 8. 详细产品需求

### 8.1 平台、安装与启动

**FR-001** 第一阶段仅支持 macOS Apple Silicon。

**FR-002** 用户通过终端完成安装和启动，并在本地浏览器中访问页面。

**FR-003** 所有正式数据在程序重启后仍应存在。

**验收标准：**

- Intel Mac 或其他系统在安装阶段明确提示暂不支持。
- 用户关闭页面但不停止服务时，后台任务继续运行。
- 用户重新启动服务后，可恢复历史数据和符合条件的中断任务。

### 8.2 模型厂商与 API Key

**FR-010** 支持 Kimi、DeepSeek、OpenAI 三个预制厂商。

**FR-011** 三个厂商的 API Key 分别保存，用户可选择当前使用厂商。

**FR-012** 页面不展示完整或脱敏 Key，仅展示厂商配置状态。

**FR-013** API Key 必须通过最小可用性请求校验后才保存。

**FR-014** Key 保存于 macOS Keychain，不写入 SQLite、普通配置文件或日志。

**FR-015** 修改已有 Key 时，新 Key 校验成功后才覆盖旧 Key；失败时旧 Key继续有效。

厂商状态：

- 未配置
- 已配置
- 校验失败
- 当前使用

校验失败类型：

- API Key 无效
- 无模型访问权限
- 余额或额度不足
- 网络不可达
- 服务暂时不可用
- 校验超时

**验收标准：**

- 至少一个厂商配置成功并被选中后，才启用音频上传。
- 校验失败时输入框保留内容，原按钮变为“重新校验”。
- 用户修改输入后，按钮恢复为“保存并校验”。

### 8.3 音频上传

**FR-020** 第一阶段仅支持 MP3、AAC。

**FR-021** 支持点击选择和拖拽多个文件。

**FR-022** 同一批文件按用户选择顺序逐个上传到本机临时目录。

**FR-023** 每条文件独立展示上传进度条和百分比。

**FR-024** 文件状态包括：等待上传、正在上传、上传完成、格式不支持、无法读取。

**FR-025** 全部文件上传完成前，“开始分析”不可用。

**FR-026** 同一文件重复添加时只保留一份并提示用户。

**FR-027** 上传前可移除文件，不支持改名或排序。

**FR-028** 当前批次开始分析后，文件列表和模型厂商锁定。

批量上传遇到不支持格式时：

- 暂停继续上传后续文件。
- 保留已上传完成文件。
- 错误文件显示“格式不支持”。
- 展示：“不支持该文件格式，请上传 MP3 / AAC 格式文件”。
- 用户移除错误文件后自动继续。

隐私文案：

> 音频仅上传至本机，不会发送到模型厂商。

### 8.4 本地 Whisper 转写

**FR-030** Kimi、DeepSeek、OpenAI 均使用同一份本地 Whisper 转写结果。

**FR-031** 文件按顺序逐个分段转写。

**FR-032** 每条文件展示等待转写、正在转写、转写完成状态及进度。

**FR-033** 进度按已处理音频分段计算，不展示不可靠的预计剩余时间。

**FR-034** 转写期间右侧不出现当前批次内容。

**FR-035** Whisper 转写完成后自动进入模型分析，无需再次操作。

### 8.5 模型分析与结果提交

**FR-040** 模型分析以整个上传批次为单位。

**FR-041** 仅发送转写文本，不发送原始音频。

**FR-042** 分析阶段包括：识别场景、生成场景内容、更新隐藏画像、保存结果。

**FR-043** 页面可显示整体分析进度，但不提前承诺一定生成某类卡片。

**FR-044** 新卡片在所有步骤成功前不进入右侧信息流。

**FR-045** 全部成功后一次性保存并展示：

- 原始音频
- 完整转写
- 卡片与详情
- 待办
- 问答会话初始记录
- 隐藏个人画像增量
- 音频历史记录

**FR-046** 分析成功后左侧立即清空，恢复默认上传区域；右侧自动定位到新批次。

**FR-047** 可显示数秒后自动消失的轻提示“分析完成”，不保留完成面板。

**FR-048** 没有生成卡片时，音频仍进入历史，画像可更新，右侧不生成空批次；左侧短暂提示本次无可展示内容。

### 8.6 信息流

**FR-050** 信息流按自然日分割。

**FR-051** 同一天按上传批次排列，最近批次在最上方。

**FR-052** 批次内不做推荐算法，采用固定顺序：

1. 会议纪要
2. 家庭教育
3. 内容推荐
4. 成长建议
5. 闲聊灵感

**FR-053** 未识别到的模块不展示。

**FR-054** 右侧默认态为空，展示“先上传音频”。

**FR-055** 右侧不展示当前批次的上传、转写和分析中状态。

### 8.7 全局待办

**FR-060** 所有批次中识别出的明确待办聚合为一个全局待办区域，固定在信息流时间线顶部。

**FR-061** 支持用户编辑、完成、删除。

**FR-062** 不支持回听。

**FR-063** 用户手动勾选完成；系统不因截止时间到期自动完成。

**FR-064** 已过期待办标红并优先显示；已完成待办弱化并放在底部。

**FR-065** 只有明确行动意图才进入待办。普通想法或探索建议不自动进入待办。

### 8.8 通用详情页框架

**FR-070** 每张内容卡片均可点击打开覆盖式详情页。

**FR-071** 详情页覆盖右侧信息流，关闭后回到原滚动位置。

**FR-072** 所有详情页共用：顶部信息、场景模块、意见反馈、完整追问记录、底部聊天输入。

**FR-073** 继续追问默认限定当前卡片及其关联音频。

**FR-074** 对话内容直接展示在详情内容下方，并用分割线区分。

**FR-075** 对话气泡最大宽度为内容区的 68%。

**FR-076** 模型必须返回结构化 JSON；前端根据场景 Schema 选择模块渲染器。

**FR-077** 缺失模块隐藏；未知模块使用通用文本或列表组件降级。

**FR-078** 保存原始模型输出、解析结果、Prompt 版本和 Schema 版本。

### 8.9 会议纪要

#### 外部卡片

- 会议纪要标签
- AI 生成会议主题
- AI 生成核心结论
- 会议开始与结束时间
- 明确决策数量
- 会议待办数量
- 查看完整纪要

不展示会议时长、表达建议或反馈按钮。

#### 详情页

- 会议摘要
- 核心结论
- 明确决策
- 会议待办
- 意见反馈
- 继续追问

不展示转写全文、不支持回听、不展示表达问题与优化建议。表达优化进入“成长建议”。

### 8.10 家庭教育

#### 外部卡片

- 家庭教育标签
- AI 生成主标题
- AI 生成核心结论
- 一个事件时显示起止时间
- 多个事件时显示“多个互动片段”

#### 详情页

一次上传中的多个家庭教育事件汇总成一张卡片，详情按事件分别展示：

- 背景信息
- 找出问题所在
- 给出切实建议
- 意见反馈
- 继续追问

人物关系、情绪原因和问题原因等推测必须明确标注。具体模型输出颗粒度在 Prompt 阶段继续优化。

### 8.11 内容推荐

#### 外部卡片

- 内容推荐标签
- AI 生成标题
- AI 生成核心内容

#### 详情页

- 这次你都听了
- 更贴合你的内容
- 意见反馈
- 继续追问

一次上传中的播客、歌曲、视频等内容汇总成一张卡片。推荐在后台使用隐藏个人画像，但页面不展示标签或画像变化。

真实内容优先；无法确认真实来源或链接时，不生成链接。

### 8.12 成长建议

#### 外部卡片

- 成长建议标签
- AI 生成标题
- AI 生成核心内容
- 仅基于本次上传的说明

#### 详情页

覆盖本次上传中识别出的全部音频场景，按方向分类，每项包括：

- 场景
- 问题
- 事实依据
- 建议
- 可选示例话术或真实资源
- 意见反馈
- 继续追问

每次上传独立生成一张成长建议卡片，不跨批次或按自然日合并。

### 8.13 闲聊灵感

#### 外部卡片

- 闲聊灵感标签
- AI 生成主标题
- AI 生成内容
- 多个灵感时显示“多个灵感片段”

#### 详情页

一次上传中的多个灵感汇总成一张卡片，每个灵感展示：

- 发生时背景
- 对话梗概
- 对话价值
- 建议推荐
- 意见反馈
- 继续追问

模型分析完整批次后自行判断内容是否值得记录。“这个不错”“这个太有价值了”等表达只作为语义参考，不作为硬触发条件。有价值才生成卡片。

只有存在明确行动意图时才同步生成待办。

### 8.14 隐藏个人画像

**FR-100** 用户第一次上传并完成分析后建立初始画像。

**FR-101** 后续每次上传完成后增量合并，不直接覆盖整份画像。

**FR-102** 第一阶段不向用户展示画像入口、标签或变化历史。

**FR-103** 内容推荐、家庭教育、成长建议和闲聊灵感可读取画像用于个性化。

画像维度包括：

- 基础属性
- 兴趣爱好
- 最近关注
- 目标与计划
- 知识与能力
- 内容偏好
- 沟通与决策偏好
- 关系与生活背景

每条画像信息至少包含：

- 值
- 可信度
- 来源音频
- 首次和最近出现时间
- 证据次数
- 用户明确表达或模型推测
- 有效、冲突或已纠正状态
- 主体 ID（用户本人或其他人物）

**FR-104** 不得把孩子、同事或朋友的兴趣和观点写入用户本人画像。

**FR-105** 清除所有历史时，画像一并删除。

### 8.15 意见反馈

**FR-110** 反馈不展示在外部卡片或详情正文中。所有详情页顶部提供“意见反馈”入口，点击后以弹窗形式完成反馈；关闭弹窗后仍停留在当前详情页。

**FR-111** 用户可选择“完全准确”或“内容不准”。

**FR-112** 弹窗默认只展示两个反馈选项。点击“完全准确”后直接保存反馈并关闭弹窗，不展示输入框或提交按钮；点击“内容不准”后才在弹窗内展开文字输入框和“提交反馈”按钮。文字输入为必填，未填写时提交按钮不可用。

**FR-113** 提交后在本机独立“意见反馈”目录生成记录。

建议目录：

`~/Library/Application Support/Audio Memory/意见反馈/`

每条反馈记录包含：

- 场景类型
- 关联音频信息
- 完整转写原文
- 完整模型生成内容
- 模型与 Prompt 版本
- 用户选择与文字反馈
- 当前详情页完整问答文字记录

数据不自动上传。

### 8.16 音频历史

**FR-120** 只收录整批分析完成的音频。

**FR-121** 按自然日分割，最近上传时间在最上方。

**FR-122** 同一上传批次中的音频排列在一起。

每条音频展示：

- 文件名
- 格式
- 文件大小
- 音频时长
- 上传时间

第一阶段不展示分析状态，不支持点击详情、编辑、删除、搜索、回听或重新分析。

失败、中断、未完成或取消的批次不进入历史，按未上传处理。

### 8.17 清除所有历史

**FR-130** 信息流、音频历史和 Prompt 设置使用统一顶部导航，并始终提供“清除所有历史”。

**FR-131** 点击后显示不可恢复操作确认弹窗，包含“取消”和“永久清除”。

**FR-132** 不要求用户输入确认文字。

永久清除范围：

- 所有历史音频
- 所有转写
- 所有卡片、详情与问答
- 全局待办
- 隐藏个人画像
- 数据库中的历史记录

保留：

- 模型厂商与 API Key 配置
- 六个场景的 Prompt 配置
- 用户主动提交的意见反馈记录

删除后首页回到“先上传音频”默认态。

### 8.18 Prompt 设置

**FR-140** Prompt 设置作为第三个一级页面，与信息流、音频历史在同一应用和同一浏览器标签内切换。

**FR-141** 第一阶段只展示六个固定场景：待办事项、会议纪要、家庭教育、内容推荐、成长建议、闲聊灵感。不支持新增、删除、排序或停用场景。

**FR-142** 每个场景只提供一个完整自然语言 Prompt 文本编辑器，默认为只读，页面仅提供“编辑”与“保存”操作。

**FR-143** 默认 Prompt 可使用“场景目标、生成条件、分析要求、详情模块、排除规则、示例”组织，但不强制为固定表单字段，用户可自由增删和重排段落。

**FR-144** 模型最终输入由三层组成：不可编辑的系统基础 Prompt、可编辑的场景 Prompt、不可编辑的通用 Schema 与格式约束。

**FR-145** 场景 Prompt 的修改不自动修改 Schema 或模型参数。详情内容通过通用 Schema 中的 `detail_sections` 动态增减。

**FR-146** 保存时保留旧 Prompt 版本快照，原子替换当前本地文件，并更新内部版本号。页面不展示版本历史操作。

**FR-147** 保存后只影响之后新分析的音频，历史卡片不重新生成。每次分析和意见反馈记录当时使用的 Prompt 版本。

**FR-148** Prompt 文件保存在 `~/Library/Application Support/AudioMemory/prompts/`，六个场景分别保存 `current.md` 和内部 `versions/` 快照。

---

## 9. 系统状态与恢复要求

### 9.1 状态清单

1. 未配置 API Key
2. API Key 校验中、成功、失败
3. 文件等待上传、上传中、上传完成
4. 格式不支持或无法读取，批次暂停
5. 本地 Whisper 转写中
6. 模型分析中
7. 整批分析成功
8. 分析失败与重试
9. 程序重启后发现未完成任务

### 9.2 自动重试

网络超时、连接中断和厂商临时错误自动重试两次，两次失败后才展示失败页面。

不自动重试：

- API Key 失效
- 额度不足
- 模型权限不足
- 文件格式异常
- 磁盘空间不足

### 9.3 从失败点继续

- 上传失败：从失败文件继续。
- 转写失败：只重试失败文件。
- 模型失败：复用已有完整转写。
- 保存失败：复用已生成结果，仅重新保存。

### 9.4 取消任务

取消前二次确认。确认后删除本批次临时音频、中间转写和模型结果，返回默认上传状态。

### 9.5 浏览器、服务和系统中断

- 只关闭浏览器页面：后台服务继续处理，重新打开页面恢复进度。
- 上传阶段服务中断：批次不可续传，重启后清理并恢复默认状态。
- 转写或分析阶段中断：保存检查点，重启后提供“继续分析”或“放弃本次任务”。
- 重启后不自动调用付费模型，必须由用户主动点击继续。
- Mac 休眠期间暂停任务，唤醒后从当前分段继续。

### 9.6 文件、磁盘和组件异常

- 文件扩展名正确但无法解码时，标记“无法读取”。
- 上传前检查本地磁盘空间；不足时不开始任务，提供“重新检查”。
- 第一阶段不设置固定音频时长上限，长音频分段处理。
- 本地 Whisper 缺失或损坏时展示可复制的修复命令。
- 开始分析前检查当前模型厂商连接和权限。

---

## 10. 设计稿与状态映射

> 研发实现以全屏可交互原型和以下实际产品界面截图为准。旧 `designs/` 文件只作为字段评审归档，其中的标题、说明和评审画板不属于产品界面。

### 10.1 完整可交互原型

[打开原型工程](</Users/liujinxin/Documents/音频Always on Demo/prototype/>)

[查看原型启动和模拟规则](</Users/liujinxin/Documents/音频Always on Demo/prototype/README.md>)

### 10.2 首页与信息流

![全屏首页与信息流](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-home-fullscreen.png>)

### 10.3 音频历史

![全屏音频历史](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-history-fullscreen.png>)

### 10.4 Prompt 设置

![全屏 Prompt 设置](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-prompts-fullscreen.png>)

### 10.5 会议详情、追问与反馈

![会议详情页顶部入口与意见反馈弹窗](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-feedback-modal-inaccurate.png>)

### 10.6 API 配置与清除历史

![API 配置](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-api-config-fullscreen.png>)

![清除历史确认](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-clear-confirm-fullscreen.png>)

### 10.7 上传、转写与分析状态

![不支持格式并暂停](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-unsupported-format-fullscreen.png>)

![本地 Whisper 转写中](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-transcribing-fullscreen.png>)

![模型分析中](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-analyzing-fullscreen.png>)

![模型分析失败与重试](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-analysis-failed-fullscreen.png>)

![中断任务恢复](</Users/liujinxin/Documents/音频Always on Demo/screenshots/product-interrupted-recovery-fullscreen.png>)

---

## 11. 用户故事与验收

### US-01 配置分析模型

作为用户，我希望分别配置三个模型厂商的 API Key，以便选择当前使用的分析厂商。

**验收：**

- 三个 Key 独立校验和保存。
- 失败 Key 不覆盖旧 Key。
- 页面不显示 Key 内容。
- 至少一个厂商配置成功后才能上传。

### US-02 上传并分析一批音频

作为用户，我希望批量上传 MP3 或 AAC，并看到每条文件进度，以便确认系统正在处理。

**验收：**

- 每条文件显示上传进度。
- 不支持格式导致批次暂停，移除后恢复。
- 上传和转写均在本地完成。
- 右侧只在整批成功后更新。

### US-03 查看信息流结果

作为用户，我希望按时间浏览不同场景的结果，以便快速找到本次音频中的价值。

**验收：**

- 信息流按自然日和批次倒序。
- 批次内按固定场景顺序。
- 无内容场景不生成空卡片。
- 每张卡片可打开对应详情页。

### US-04 管理全局待办

作为用户，我希望集中编辑、完成和删除从音频中识别的待办。

**验收：**

- 待办固定在信息流顶部。
- 已过期标红，已完成弱化置底。
- 只有明确行动意图才生成待办。

### US-05 追问当前结果

作为用户，我希望围绕当前卡片继续提问，以便理解或扩展模型输出。

**验收：**

- 追问默认限定当前卡片及关联音频。
- 完整问答在详情页持续展示。
- 反馈提交时问答一并保存。

### US-06 反馈模型结果

作为用户，我希望标记内容准确或不准确并说明原因，以便后续优化。

**验收：**

- 详情页顶部展示意见反馈入口，点击后打开弹窗，详情正文不再铺设反馈模块。
- 弹窗默认不展示文字输入；选择“内容不准”后才展开必填输入框和提交按钮。
- 选择“完全准确”后直接在本机保存反馈并关闭弹窗。
- 记录场景、音频、原文、输出、版本、反馈和问答。
- 反馈只保存在本机。

### US-07 查看音频历史

作为用户，我希望看到已完成分析的历史音频，以确认本地保存内容。

**验收：**

- 只展示已完成整批分析的音频。
- 自然日和上传批次倒序排列。
- 页面无管理操作和状态字段。

### US-08 恢复中断任务

作为用户，我希望程序重启后继续已完成上传的转写或分析任务，以避免重复处理长音频。

**验收：**

- 转写或模型阶段中断可从检查点恢复。
- 上传阶段中断不可续传并自动清理。
- 付费模型不会在重启后自动调用。

### US-09 清除本地历史

作为用户，我希望一次性清除历史音频和生成内容，以便重置系统。

**验收：**

- 操作前弹窗确认。
- 删除历史、待办和隐藏画像。
- 保留 API Key 和意见反馈。
- 删除后回到默认首页。

---

## 12. 成功指标（待评审建议）

第一阶段是用户体验验证版，建议先用本地反馈和测试记录衡量，不预设正式商业指标。

### 12.1 主指标

**有效结果批次率**：完成分析的批次中，至少有一张卡片被测试用户认为有价值的比例。

### 12.2 次级指标

- 整批分析成功率。
- 上传到首批结果出现的总耗时。
- 各场景卡片生成率。
- “完全准确”反馈比例。
- “内容不准”反馈的主要问题分布。
- 单批次被打开的详情页数量。
- 继续追问使用率。
- 用户次日或七日内再次上传音频的比例。
- 不同厂商在相同评测样本上的结果差异。

### 12.3 保护指标

- 原始音频发送到模型厂商的次数必须为 0。
- API Key 写入普通文件或日志的次数必须为 0。
- 未完成批次进入正式历史的次数必须为 0。
- 清除历史后残留正式音频和画像的次数必须为 0。

具体目标值在首轮真实体验测试后确定。

---

## 13. 依赖与风险

### 13.1 关键依赖

- 本地 Whisper 的安装、模型规格、性能和中文准确率。
- 长音频分段、时间戳和说话人区分能力。
- Kimi、DeepSeek、OpenAI API 的模型可用性与结构化输出能力。
- 各场景 Prompt 与 JSON Schema 的设计质量。
- macOS Keychain、本地数据库和文件系统可靠性。

### 13.2 主要风险与缓解

#### 风险：长音频转写耗时过长

- 分段处理并展示真实进度。
- 支持检查点和重启恢复。
- 安装阶段提前准备 Whisper。

#### 风险：人物和观点归属错误

- 引入说话人主体 ID。
- 画像信息保存来源、可信度和推测状态。
- 家庭与成长建议明确区分事实和推测。

#### 风险：模型 JSON 不稳定

- 使用 Schema 校验、有限重试和降级渲染。
- 保存原始输出、Prompt 和 Schema 版本。

#### 风险：推荐真实内容时产生幻觉

- 真实内容优先。
- 无法确认时不生成链接。
- 后续再接入可验证的联网检索或内容目录。

#### 风险：隐藏画像错误影响推荐

- 保存证据和可信度。
- 不将单次偶然信息直接升级为稳定画像。
- 使用反馈记录修正后续 Prompt 和画像合并规则。

#### 风险：保留意见反馈与“清除所有历史”的预期冲突

- 确认弹窗明确列出保留项。
- 反馈目录与正式数据目录隔离。
- 后续评审是否增加单独清除反馈入口。

---

## 14. 开放问题（评审时逐项确认）

1. 第一阶段是否仍需提供“全局查询音频内容”？目前只保留卡片内追问，音频历史不支持搜索。
2. Whisper 具体模型规格、量化版本和首次安装包体积尚未确定。
3. 第一阶段是否必须支持说话人分离；如果支持，最低准确性要求是什么。
4. 卡片所属自然日使用音频文件时间、上传时间，还是从内容推断的实际发生时间。
5. Kimi、DeepSeek、OpenAI 各自使用的默认模型名称尚未确定。
6. 场景 Prompt 的具体输出颗粒度、长度限制和成本预算尚未确定。
7. 成功指标目标值需要在首轮体验测试后设定。
8. 是否需要为意见反馈提供独立清除入口。

---

## 15. 决策记录

| 决策 | 第一阶段结论 |
|---|---|
| 音频来源 | 只支持用户上传已有音频 |
| 平台 | macOS Apple Silicon |
| 转写 | 三个厂商统一使用本地 Whisper |
| 分析厂商 | Kimi、DeepSeek、OpenAI |
| API Key | 用户分别配置，校验成功后存入 Keychain |
| 音频格式 | MP3、AAC |
| 信息流 | 自然日 + 上传批次，最近在上 |
| 处理中结果 | 只在左侧显示，右侧不展示半成品 |
| 卡片排序 | 会议、家庭、内容、成长、灵感 |
| 待办 | 全局置顶，支持编辑、完成、删除 |
| 个人画像 | 第一次上传建立，后台使用，不向用户展示 |
| 音频历史 | 只读列表，只收录完整成功批次 |
| 意见反馈 | 本机独立目录，保存原文、输出、反馈和问答 |
| 清除历史 | 删除正式历史和画像，保留 Key 与意见反馈 |
| 模型对比版 | 后续独立设计，不纳入本版 |

---

## 16. 评审方式

建议按以下顺序评审：

1. 目标与非目标
2. 核心用户流程
3. 首页与系统状态
4. 全局待办
5. 五类内容卡片
6. 隐藏个人画像
7. 音频历史与清除逻辑
8. 成功指标
9. 开放问题

评审通过后：

- 本文升级为 v1.0。
- 冻结第一阶段范围。
- 进入技术架构、数据模型、Prompt 框架和开发计划阶段。
