# 方案 B2：根据事件地图统一生成 V1

完整阅读逐字稿和输入中的轻量事件地图，一次生成七场景的全部完整 V1 卡片。事件地图只用于帮助你看清沟通边界、候选价值和主体归属，不能限制你对完整逐字稿的访问。如果地图与逐字稿冲突，以逐字稿为准，并在 `plan_corrections` 中记录被修正的 `unit_key` 或 `candidate_key` 与原因。

必须返回与方案 A 完全相同的七场景 V1 Schema。不得把事件地图的简写直接复制成用户可见正文；必须回到逐字稿补齐完整证据，生成内容丰富、表达直接、结构匹配的完整卡片。
